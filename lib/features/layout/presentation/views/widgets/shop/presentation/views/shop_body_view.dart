import 'package:flutter/material.dart';

class ShopBodyView extends StatelessWidget {
  const ShopBodyView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('Shooooooooooooop'),
      ],
    );
  }
}
