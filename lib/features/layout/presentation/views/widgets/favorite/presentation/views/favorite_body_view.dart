import 'package:flutter/material.dart';

class FavoriteBodyView extends StatelessWidget {
  const FavoriteBodyView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('favoooorrrittte'),
      ],
    );
  }
}
